# Password for the /admin dashboard (default is "admin123" if unset — change this!)
ADMIN_PASSWORD=change-me-to-a-strong-password

# Secret used to sign admin session cookies (any long random string)
ADMIN_SECRET=some-long-random-secret-string
